rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 지도안 PDF는 서버(API + Admin SDK)를 통해서만 업로드/삭제하므로 클라이언트 직접 접근은 차단합니다.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
